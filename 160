1. 2 runners, both run the length of M + N  O(M + N), O(!)

class Solution(object):
    def getIntersectionNode(self, headA, headB):
        if headA is None or headB is None:
            return None
        p1 = headA
        p2 = headB
        while p1 != p2 and p1 and p2:
            p1 = p1.next
            p2 = p2.next
            if p1 == p2:
                break
            if p1 is None:
                p1 = headB
            if p2 is None:
                p2 = headA
        return p1

* condition tricky: after p1 = p1.next, p2 = p2.next statement also need to check if p1 == p2 inside loop
