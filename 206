both O(n), pre - node if node == None, get to terminal, return pre keep record of node.next as temporary value then reverse from pre -> node to node -> pre

class Solution(object):
    def reverseList(self, head):
        pre = None
        node = head
        while node is not None:
            next = node.next
            node.next = pre
            pre = node
            node = next
        return pre

"def helper(pre, node):
    if node is None:
        return pre
    next = node.next
    node.next = pre
    return helper(node, next)
class Solution(object):
    def reverseList(self, head):
        return helper(None, head)"
